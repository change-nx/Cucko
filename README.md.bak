# Cucko
> 基于Napcat的PHP框架

## 前言
> 作者∶长歌(3355236800)
> 群聊∶323843010
> 由于本人常年在校,有问题建议群内询问

## 配置准备
- Napcat
- PHP7.4

## 框架配置
打开info.php
```php
<?php
return [
    "url" => "http://127.0.0.1:3000",
    "url_token" => "",
    "http_token" => ""
];
```
url -> HTTP服务器地址
url_token -> HTTP服务器Token
http_token -> HTTP客户端Token(选填)

## 插件
> 存放路径∶plugin/

示例∶test.php
```php
<?php
if (消息来源 != "群聊") return;

if (消息 == "测试") {
    群(群号,text("你好啊"));
}
```